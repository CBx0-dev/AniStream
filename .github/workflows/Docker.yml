name: Build and Push Containers

on:
  workflow_dispatch:
    inputs:
      push_latest:
        description: 'Also tag and push as latest'
        type: boolean
        default: true

permissions:
  contents: write
  packages: write

jobs:
  next-tag:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.compute.outputs.tag }}
      owner: ${{ steps.owner.outputs.lower }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Lowercase repository owner
        id: owner
        run: echo "lower=${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_OUTPUT"

      - name: Compute next build number
        id: compute
        run: |
          git fetch --tags
          last=$(git tag -l 'docker-build-*' | sed 's/docker-build-//' | sort -n | tail -1)
          next=$(( ${last:-0} + 1 ))
          echo "tag=${next}" >> "$GITHUB_OUTPUT"
          git tag "docker-build-${next}"
          git push origin "docker-build-${next}"

  build:
    needs: next-tag
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - name: api
            dockerfile: api.Dockerfile
          - name: worker
            dockerfile: worker.Dockerfile
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push ${{ matrix.name }}
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          push: true
          tags: |
            ghcr.io/${{ needs.next-tag.outputs.owner }}/anistream-${{ matrix.name }}:${{ needs.next-tag.outputs.tag }}
            ghcr.io/${{ needs.next-tag.outputs.owner }}/anistream-${{ matrix.name }}:${{ github.sha }}
            ${{ inputs.push_latest && format('ghcr.io/{0}/anistream-{1}:latest', needs.next-tag.outputs.owner, matrix.name) || '' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max